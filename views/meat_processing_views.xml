<odoo>
    <data>
        <!-- Vista de Árbol de Órdenes de Procesamiento -->
        <record id="view_meat_processing_order_tree" model="ir.ui.view">
            <field name="name">meat.processing.order.tree</field>
            <field name="model">meat.processing.order</field>
            <field name="arch" type="xml">
                <tree string="Órdenes de Procesamiento de Carne">
                    <field name="name"/>
                    <field name="order_date"/>
                    <field name="customer_id"/>
                    <field name="state"/>
                    <field name="total_amount"/>
                </tree>
            </field>
        </record>

        <!-- Vista de Formulario de Órdenes de Procesamiento -->
        <record id="view_meat_processing_order_form" model="ir.ui.view">
            <field name="name">meat.processing.order.form</field>
            <field name="model">meat.processing.order</field>
            <field name="arch" type="xml">
                <form string="Orden de Procesamiento de Carne">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="order_date"/>
                            <field name="customer_id"/>
                            <field name="state" readonly="1"/>
                            <field name="total_amount" readonly="1"/>
                            <field name="notes"/>
                        </group>
                        <group>
                            <field name="order_line_ids">
                                <tree string="Líneas de la Orden">
                                    <field name="product_id"/>
                                    <field name="quantity"/>
                                    <field name="unit_price"/>
                                    <field name="subtotal" readonly="1"/>
                                </tree>
                            </field>
                        </group>
                        <footer>
                            <button string="Confirmar" type="object" name="action_confirm" class="oe_highlight"/>
                            <button string="Hecho" type="object" name="action_done" class="oe_highlight"/>
                            <button string="Cancelar" type="object" name="action_cancel" class="oe_highlight"/>
                            <button string="Restablecer a Borrador" type="object" name="action_set_to_draft" class="oe_highlight"/>
                        </footer>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Acción de Ventana para Órdenes de Procesamiento -->
        <record id="action_meat_processing_orders" model="ir.actions.act_window">
            <field name="name">Órdenes de Procesamiento de Carne</field>
            <field name="res_model">meat.processing.order</field>
            <field name="view_mode">tree,form</field>
            <field name="view_id" ref="view_meat_processing_order_tree"/>
        </record>
    </data>
</odoo>
